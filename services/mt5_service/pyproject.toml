[project]
name = "mt5-service"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "influxdb-client",
    "MetaTrader5",
    "prometheus-fastapi-instrumentator>=7.0.0",
    "prometheus-client>=0.20.0",
]

[tool.uv.sources]
# Add workspace dependencies if needed later
